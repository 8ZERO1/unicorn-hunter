/** @type {import('next').NextConfig} */
const nextConfig = {
  // Removed deprecated optimizeFonts experimental option
  // This was causing the build warning and is no longer needed in Next.js 15
}

module.exports = nextConfig